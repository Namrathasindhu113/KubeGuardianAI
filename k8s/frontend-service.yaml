apiVersion: v1
kind: Service

metadata:
  name: kubeguardian-frontend-service

spec:
  selector:
    app: kubeguardian-frontend

  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173

  type: NodePort